<%- include('partials/header') %> 
<%- include('partials/navbar') %> 

<div class="px-4 pb-10">
    <div class="max-w-md mx-auto mt-6 md:mt-10 bg-white rounded-xl md:rounded-2xl shadow-lg md:shadow-xl overflow-hidden border border-gray-100">
        
        <div class="bg-[#00B900] p-6 md:p-8 text-center text-white relative">
            <div class="w-16 h-16 bg-white rounded-2xl mx-auto flex items-center justify-center mb-4 shadow-inner">
                <i class="fab fa-line text-[#00B900] text-4xl"></i>
            </div>
            <h2 class="text-xl md:text-2xl font-bold">เพิ่มเพื่อนบอท WELLOFF</h2>
            <p class="text-xs md:text-sm opacity-90 mt-1.5">สแกนเพื่อเริ่มใช้งานระบบจองแชร์</p>
        </div>

        <div class="p-6 md:p-8 text-center">
            
            <div class="border-2 border-dashed border-gray-200 rounded-2xl p-4 inline-block mb-6 bg-gray-50">
                <img src="https://qr-official.line.me/gs/M_753sovxz_GW.png?oat_content=qr" alt="LINE QR Code" class="w-40 h-40 md:w-48 md:h-48 mx-auto object-contain">
            </div>

            <a href="https://lin.ee/REWGoHk" class="flex items-center justify-center gap-2 text-gray-600 hover:text-green-600 font-bold mb-6 transition-colors bg-gray-50 hover:bg-green-50 py-3 rounded-xl border border-gray-100">
                <i class="fas fa-user-plus text-lg"></i> เพิ่มเพื่อนผ่านลิงก์
            </a>

            <div class="bg-yellow-50 rounded-xl p-4 md:p-5 text-left text-xs md:text-sm border border-yellow-100">
                <h3 class="font-bold text-yellow-800 mb-3 flex items-center gap-2 text-sm md:text-base">
                    <i class="fas fa-magic text-yellow-600"></i> วิธีเปิดใช้งานบอท:
                </h3>
                <ol class="text-yellow-700 space-y-3 list-decimal list-inside ml-1">
                    <li><span class="font-bold text-yellow-900">เพิ่มเพื่อน</span> บอทให้เรียบร้อย</li>
                    <li><span class="font-bold text-yellow-900">ดึงบอทเข้ากลุ่มแชร์</span> ที่คุณต้องการให้บอททำงาน</li>
                    <li>พิมพ์คำสั่ง <code class="bg-yellow-200 px-2 py-0.5 rounded font-bold text-yellow-900">/getid</code> ลงในกลุ่มนั้น</li>
                    <li>นำรหัสที่บอทตอบกลับ มาตั้งค่าในหน้า <a href="/line" class="underline font-bold text-blue-600 hover:text-blue-800 transition">จัดการบอทไลน์</a></li>
                </ol>
            </div>

            <div class="mt-8">
                <a href="/line" class="text-gray-400 hover:text-gray-600 text-sm font-medium flex items-center justify-center gap-1 transition-colors">
                    <i class="fas fa-chevron-left text-xs"></i> กลับไปหน้าจัดการบอทไลน์
                </a>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') %>